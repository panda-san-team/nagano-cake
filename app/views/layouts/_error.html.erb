<% if obj.errors.any? %>
<p class="text-danger"><%= obj.errors.count %>件のエラーが発生しています。</p>
 <ul>
  <% obj.errors.full_messages.each do |message| %>
   <li class="text-danger"><%= message %></li>
  <% end %>
 </ul>
<% end %>