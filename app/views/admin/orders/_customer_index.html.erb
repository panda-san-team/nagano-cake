<table class="table">
  <thead class="thead-light">
    <tr>
      <th>購入日時</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% customer_orders.each do |order| %>
    <tr>
      <td class="table-link" data-href="/admin/orders/<%= order.id %>">
        <%= link_to admin_order_path(order), class: "link" do %>
        <%= order.created_at.strftime("%Y/%m/%d %H:%M:%S") %>
        <% end %>
      </td>
      <td><%= order.sum_amount %></td>
      <td><%= order.status_i18n %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<script>
jQuery(document).on("turbolinks:load", function() {
  $(".table-link").on('click', function() {
      window.location = $(this).data("href");
  });
});
</script>
<%= paginate customer_orders, remote: true %>
